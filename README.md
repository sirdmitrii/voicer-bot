# 🤖 Telegram Call Analyzer Bot

Этот бот автоматически анализирует аудиозаписи звонков (MP3, OGG, WAV) с помощью **AI (GPT-4o Audio)** и сохраняет детальную оценку работы менеджера в Google Таблицу.

## 🚀 Возможности
*   **AI-анализ**: Оценивает интонацию, приветствие, выявление потребностей, работу с возражениями и другие параметры.
*   **Google Sheets**: Автоматически создает структуру таблицы и добавляет новые записи.
*   **Очередь обработки**: Поддерживает массовую загрузку файлов (отправьте хоть 100 аудио сразу).
*   **Проверка дубликатов**: Если файл уже анализировался, бот спросит, нужно ли его перезаписать.

---

## 🛠 Установка

### 1. Требования
*   Python 3.8+
*   FFmpeg (установленный в системе)

### 2. Клонирование репозитория
```bash
git clone https://github.com/sirdmitrii/voicer-bot.git
cd voicer-bot
```

### 3. Установка библиотек
```bash
pip install -r requirements.txt
```

*(Если файла requirements.txt нет, установите вручную: `pip install python-telegram-bot openai gspread oauth2client requests`)*

---

## 🔑 Настройка ключей (Credentials)

Проект использует один файл `credentials.json` для всех ключей. Это сделано для удобства и безопасности (файл добавлен в `.gitignore`).

1. Создайте файл `credentials.json` в корневой папке.
2. Вставьте в него следующий контент (заполнив своими данными):

```json
{
  "type": "service_account",
  "project_id": "YOUR_PROJECT_ID",
  "private_key_id": "YOUR_KEY_ID",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "YOUR_SERVICE_ACCOUNT_EMAIL",
  "client_id": "...",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "...",
  "universe_domain": "googleapis.com",

  "telegram_token": "ВАШ_ТЕЛЕГРАМ_ТОКЕН",
  "openai_api_key": "ВАШ_OPENAI_KEY",
  "google_sheet_name": "ИМЯ_ВАШЕЙ_ТАБЛИЦЫ"
}
```

> **Важно**: Первую часть (ключи Google Service Account) вы скачиваете из Google Cloud Console. Просто добавьте в этот JSON три последних поля (`telegram_token`, `openai_api_key`, `google_sheet_name`).

---

## ▶️ Запуск

```bash
python bot.py
```

После запуска отправьте боту аудиофайл или перешлите голосовое сообщение.

---

## 🛡 Безопасность
Файл `credentials.json` содержит секретные ключи. **Никогда не передавайте его третьим лицам** и не загружайте в публичные репозитории.
